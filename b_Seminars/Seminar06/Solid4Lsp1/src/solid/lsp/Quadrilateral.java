package Solid4Lsp1.src.solid.lsp;

public abstract class Quadrilateral {
    // Creating an abstract class which will further be extended by Rectangle and Square
    // An interface could also be created instead of an abstract class as in this object
    // nothing other than abstract method is realized.
    public abstract int getArea();
}
